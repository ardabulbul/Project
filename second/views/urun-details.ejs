<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>urunler</title>
  </head>
  <body>
    <h1><%= name %></h1>
    <img src="/images/<%= imageUrl || '' %>" alt width="100" />
    <p>fiyat: <%= price %></p>
    <p>
      <% if (isActive) { %>
      <span>urun satista</span>
      <% } else { %>
      <span>urun satista degil</span>
      <% } %>
    </p>
  </body>
</html>
